// Copy to secrets.h (gitignored) for bench convenience. Never commit
// real credentials — same policy as the Feather and Metro builds.
#pragma once

#define WIFI_SSID "your-iphone-hotspot"
#define WIFI_PASS "hotspot-password"

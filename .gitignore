# PlatformIO
.pio/
.vscode/

# Credentials — never committed (copy secrets.example.h -> secrets.h)
**/secrets.h

# Built / backup NINA module images (reproducible or per-board dumps —
# vendored data/*.bin inputs stay tracked)
firmware/nina-fw-airlift/NINA_W102*.bin
firmware/nina-fw-airlift/x509_crt_bundle
*-stock-backup.bin

# Local tool settings
.claude/settings.local.json

# OS noise
Thumbs.db
.DS_Store

# Generated web UI asset header (tools/webui/build_assets.py)
firmware/pynt/rover/web_assets.h
